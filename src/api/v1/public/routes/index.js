const authEndpoints = require("./auth");
const pingEndpoints = require("./ping");
const userEndpoints = require("./user");



module.exports = {
    authEndpoints,
    pingEndpoints,
    userEndpoints,
};
