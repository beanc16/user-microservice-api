{
    "version": 2,
    "builds": [{
        "src": "./index.js",
        "use": "@vercel/node",
        "config": {
            "env": {
                "NPM_CONFIG_USERCONFIG": "/vercel/.npmrc"
            }
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "/"
    }]
}